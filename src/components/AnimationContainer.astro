---
export interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class={`group border-custom flex h-16 w-16 cursor-pointer items-center justify-center border ${className}`}
>
  <slot />
</div>