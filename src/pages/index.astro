---
import { Image } from 'astro:assets';
import type { SanityDocument } from '@sanity/client';
import { loadQuery } from '../utils/loadQuery';
import { urlFor } from '../utils/sanityImageUrl';

import Layout from '../layouts/Layout.astro';
import WhatsAppIcon from '../assets/whatsapp-icon.svg';
import ArrowDown from '../assets/arrow-down.svg';
import ArrowRight from '../assets/arrow-right.svg';
import PrimaryButton from '../components/PrimaryButton.astro';
import HerniaButton from '../components/HerniaButton.astro';
import medic from '../assets/dr-main.png';
import TriangleBarsAnimation from '../components/TriangleBarsAnimation.astro';
import SquaresAnimation from '../components/SquaresAnimation.astro';
import HeartAnimation from '../components/HeartAnimation.astro';
import PolygonAnimation from '../components/PolygonAnimation.astro';
import contact from '../config/contact';

const { data: testimonials } = await loadQuery<SanityDocument[]>({
  query: `*[_type == "review"] | order(priority asc)[0...3] {
      _id,
      name,
      description,
      priority,
      image,
      rating
    }`,
});
const reviews = testimonials.map((review) => ({
  imageUrl: review?.image
    ? urlFor(review.image)?.auto('format').width(120).height(120).url()
    : null,
  name: review.name.replace(' ', '<br />'),
  description: review.description,
  rating: Math.max(Math.min(review?.rating || 5, 5), 1), // Clamp rating between 1 and 5
}));
---

<Layout title="Hernia Center - Chirurgie minim invazivă a herniilor">
  <section class="max-w-full px-4 md:px-0">
    <div class="relative">
      <div class="grid grid-cols-2 md:grid-cols-8">
        <div class="hidden aspect-square md:block"></div>
        <div class="border-custom md:aspect-square md:border-r md:border-b md:border-l"></div>
        <div class="border-custom md:aspect-square md:border-r md:border-b"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="hidden aspect-square md:block"></div>
        <div class="hidden aspect-square md:block"></div>
        <div class="border-custom aspect-square md:border-r md:border-b md:border-l"></div>
        <div class="border-custom aspect-square md:border-r md:border-b"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="hidden aspect-square md:block"></div>
        <div class="hidden aspect-square md:block"></div>
        <div class="border-custom aspect-square border-b md:border-r md:border-l"></div>
        <div class="border-custom aspect-square border-b md:border-r"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="border-custom hidden aspect-square border-r border-b md:block"></div>
        <div class="hidden aspect-square md:block"></div>
        <div class="hidden aspect-square md:block"></div>

        <div class="border-custom block aspect-square border-r border-b border-l md:hidden"></div>
        <div class="border-custom block aspect-square border-r border-b md:hidden"></div>
        <div class="block aspect-square md:hidden"></div>
        <div class="block aspect-square md:hidden"></div>
        <div class="block aspect-square md:hidden"></div>
        <div class="border-custom block aspect-square border-r border-l md:hidden"></div>

        <div class="hidden aspect-square md:block"></div>
        <div class="hidden aspect-square md:block"></div>
        <div class="hidden aspect-square md:block"></div>
        <div class="hidden aspect-square md:block"></div>
        <div class="hidden aspect-square md:block"></div>
      </div>

      <div
        class="grid-cols2 absolute inset-0 grid w-full grid-rows-5 md:grid-cols-8 md:grid-rows-4"
      >
        <h1
          class="col-span-2 col-start-1 row-span-3 row-start-1 self-start leading-[0.8] font-bold tracking-tight md:col-span-3 md:col-start-3 md:text-6xl"
          style="font-size: clamp(3rem, 4.6875vw, 5.625rem);transform: translateY(clamp(2.35rem, 8.65vw, 12.2rem));"
        >
          <p class="hidden md:block"></p>
          <p>chirurgia</p>
          <p class="text-nowrap">minim invazivă</p>
          <p>a herniilor</p>
        </h1>

        <div class="col-span-2 col-start-1 row-span-1 row-start-4 md:col-span-3 md:col-start-5">
        </div>

        <div
          class="relative col-span-2 col-start-1 row-span-2 row-start-3 overflow-visible md:static md:col-span-3 md:col-start-5 md:row-span-4 md:row-start-1 md:overflow-hidden"
        >
          <Image
            src={medic}
            alt="Hernia Center"
            class="absolute -bottom-[5%] left-1/2 h-[110%] w-auto -translate-x-1/2 object-contain md:static md:h-full md:w-full md:translate-x-0 md:scale-140"
          />
        </div>
        <div
          class="col-span-2 col-start-1 row-span-1 row-start-5 pt-6 text-left md:col-span-3 md:col-start-2 md:row-span-3 md:row-start-4 md:pt-0"
        >
          <span class="text-heading-number text-5xl leading-12 font-bold md:text-6xl lg:leading-14"
            >01.</span
          >
          <h2 class="text-4xl font-bold tracking-tight md:text-5xl lg:text-7xl">
            <span class="hidden md:block">tipuri de hernii</span>
            <span class="block md:hidden">tipuri de<br />hernii</span>
          </h2>
        </div>

        <div
          class="col-span-2 col-start-1 row-start-2 mt-10 md:col-span-1 md:col-start-3 md:row-start-3 md:mt-0"
        >
          <PrimaryButton text="WhatsApp" icon={WhatsAppIcon} href={contact.whatsapp} />
        </div>
      </div>
    </div>

    <!-- Types of Hernias Section -->
    <section>
      <div class="relative grid grid-cols-2 md:grid-cols-8">
        <div class="hidden aspect-square md:block"></div>
        {
          [
            {
              title: 'hernie inghinală',
              description:
                'Cea mai frecventa hernie, apare în zona inghinală și poate fi tratată rapid și sigur printr-o intervenție minim invazivă.',
            },
            {
              title: 'hernie ombilicală',
              description:
                'Se formează în zona buricului, iar tratamentul modern elimină hernia și redă aspectul natural al buricului.',
            },
            {
              title: 'hernie femurală',
              description:
                'Apare sub pliul inghinal, mai frecvent la femei, și poate fi tratată eficient pentru a preveni durerea și complicațiile.',
            },
            {
              title: 'hernie hiatală',
              description:
                'Poate provoca arsuri si reflux, iar tratamentul potrivit reda confortul digestiv.',
            },
            {
              title: 'hernie incizională',
              description:
                'Apar la nivelul inciziilor postoperatorii, pot fi complexe, iar tratamentul restabileste structura peretelui abdominal',
            },
            {
              title: 'hernii rare',
              description:
                'Hernii de linie alba, hernie Spiegeliana, diastazisul dreptilor abdominali, hernia obturatorie, hernia sportivului',
            },
          ].map(({ title, description }, index, items) => (
            <HerniaButton
              number={index + 1}
              title={title}
              description={description}
              borderClasses={`border-t border-b-0 border-l md:border-b ${
                index === items.length - 1
                  ? 'border-r'
                  : index % 2 === 1
                    ? 'border-r md:border-r-0'
                    : ''
              }`}
              hasArrowClass={true}
              titleBreak={true}
              innerDivFullWidth={true}
            />
          ))
        }
        <div class="hidden aspect-square md:block"></div>
        <div class="hidden aspect-square md:block"></div>
        <div class="border-custom hidden aspect-square border-b border-l md:block"></div>
        <div class="border-custom hidden aspect-square border-b border-l md:block"></div>
        <button
          class="group primary-bg col-span-2 flex aspect-[2/1] items-center justify-center transition-colors hover:opacity-90 md:aspect-auto"
          onclick="location.href='/interventii'"
        >
          <span class="responsive-button-text flex items-center text-white">
            citește mai mult
            <ArrowRight
              class="mt-1 ml-4 scale-80 transition-all duration-300 ease-out group-hover:translate-x-3"
            />
          </span>
        </button>
        <div class="border-custom hidden aspect-square border-b border-l md:block"></div>
        <div class="border-custom hidden aspect-square border-r border-b border-l md:block"></div>
        <div class="hidden aspect-square md:block"></div>
      </div>
      <div
        id="hernina-type-modal"
        class="border-custom hernia-container primary-bg absolute hidden cursor-pointer border p-7"
      >
        <div class="flex justify-between">
          <div id="hernia-number" class="hernia-title"></div>
          <ArrowDown class="arrow-icon" />
        </div>
        <h3 id="hernia-title" class="hernia-text"></h3>
        <h3 id="hernia-description" class="hernia-text"></h3>
      </div>
    </section>

    <div class="relative mt-8 overflow-hidden">
      <div
        id="scroll-title-wrapper"
        class="stars-color pointer-events-none leading-none font-bold whitespace-nowrap opacity-30 select-none"
        style="--scroll-shift:0;"
      >
        <span
          id="scroll-title"
          class="scroll-title mx-8 inline-block translate-x-0 will-change-transform md:mx-16"
          >hernia&nbsp;center</span
        >
      </div>
    </div>
  </section>

  <!-- Benefits Section -->
  <section class="mt-16 max-w-full px-4 md:mt-32 md:px-0">
    <div class="relative grid grid-cols-2 md:grid-cols-8">
      <div class="hidden aspect-square md:block"></div>
      <div class="col-span-1 col-start-1 pt-6 text-left md:col-span-3 md:pt-0">
        <span class="text-heading-number text-5xl leading-12 font-bold md:text-6xl lg:leading-14"
          >02.</span
        >
        <h2 class="text-4xl font-bold tracking-tight md:text-5xl lg:text-7xl">beneficii</h2>
      </div>
      <div class="border-custom hidden aspect-square border-t border-l md:block"></div>
      <div class="border-custom hidden aspect-square border-t border-l md:block"></div>
      <div class="border-custom aspect-square border-t border-r border-l"></div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div
        class="hernia-button benefits border-custom hernia-container flex aspect-square flex-col border-t border-l md:col-span-3 md:row-span-3"
        data-number="01"
        data-title="recuperare rapidă"
        data-description="Tehnici moderne minim invazive, incizii mici, durere redusă, spitalizare scurtă, rezultat estetic superior."
      >
        <div class="flex justify-between">
          <div class="hernia-title font-semibold">01</div>
          <ArrowDown class="block md:hidden" />
          <div class="hidden md:block">
            <TriangleBarsAnimation />
          </div>
        </div>
        <h3 class="benefits-title">recuperare<br />rapidă</h3>
        <h4 class="hernia-text benefits-description hidden md:block">
          Tehnici moderne minim invazive, incizii mici, durere redusă, spitalizare scurtă, rezultat
          estetic superior.
        </h4>
      </div>
      <div
        class="hernia-button benefits border-custom hernia-container flex aspect-square flex-col border-t border-r border-l md:col-span-3 md:row-span-3"
        data-number="02"
        data-title="aparatură de ultimă generație"
        data-description="Tehnologii atraumatice pentru chirurgie robotică și laparoscopică, imagistică avansată (ecografie, computer tomograf, rezonanță magnetică)."
      >
        <div class="flex justify-between">
          <div class="hernia-title font-semibold">02</div>
          <ArrowDown class="block md:hidden" />
          <div class="hidden md:block">
            <SquaresAnimation />
          </div>
        </div>
        <h3 class="benefits-title">aparatură de<br />ultimă generație</h3>
        <h4 class="hernia-text benefits-description hidden md:block">
          Tehnologii atraumatice pentru chirurgie robotică și laparoscopică, imagistică avansată
          (ecografie, computer tomograf, rezonanță magnetică).
        </h4>
      </div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div
        class="hernia-button benefits border-custom hernia-container flex aspect-square flex-col border-t border-b border-l md:col-span-3 md:row-span-3"
        data-number="03"
        data-title="spital modern"
        data-description="Saloane confortabile dotate cu baie proprie, TV, Wi-Fi, spații de relaxare, restaurant și parcare, pentru o experiență medicală plăcută și sigură."
      >
        <div class="flex justify-between">
          <div class="hernia-title font-semibold">03</div>
          <ArrowDown class="block md:hidden" />
          <div class="hidden md:block">
            <HeartAnimation />
          </div>
        </div>
        <h3 class="benefits-title">spital<br />modern</h3>
        <h4 class="hernia-text benefits-description hidden md:block">
          Saloane confortabile dotate cu baie proprie, TV, Wi-Fi, spații de relaxare, restaurant și
          parcare, pentru o experiență medicală plăcută și sigură.
        </h4>
      </div>
      <div
        class="hernia-button benefits border-custom hernia-container flex aspect-square flex-col border-t border-r border-b border-l md:col-span-3 md:row-span-3"
        data-number="04"
        data-title="discreție și confort"
        data-description="Respectăm intimitatea fiecărui pacient și oferim o experiență medicală discretă și confortabilă, cu programări rapide și contact direct cu medicul chirurg."
      >
        <div class="flex justify-between">
          <div class="hernia-title font-semibold">04</div>
          <ArrowDown class="block md:hidden" />
          <div class="hidden md:block">
            <PolygonAnimation />
          </div>
        </div>
        <h3 class="benefits-title">discreție<br />și confort</h3>
        <h4 class="hernia-text benefits-description hidden md:block">
          Respectăm intimitatea fiecărui pacient și oferim o experiență medicală discretă și
          confortabilă, cu programări rapide și contact direct cu medicul chirurg.
        </h4>
      </div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div class="hidden aspect-square md:block"></div>
      <div class="col-span-1 col-start-1 pt-6 text-left md:col-span-3">
        <span class="text-heading-number text-5xl leading-12 font-bold md:text-6xl lg:leading-14"
          >03.</span
        >
        <h2 class="text-4xl font-bold tracking-tight md:text-5xl lg:text-7xl">
          <span class="hidden md:block">ce zice lumea</span>
          <span class="block md:hidden">ce zice<br />lumea</span>
        </h2>
      </div>
      <div class="border-custom aspect-square border-r border-b border-l md:hidden"></div>
    </div>
  </section>

  <section id="recenzii">
    <div class="flex flex-col flex-wrap pt-2 md:flex-row md:px-(--one-eigth-width)">
      {
        reviews?.map((review: any, index: number) => (
          <div
            class={`border-custom flex flex-col border-t-0 border-r-0 border-b border-l-0 p-8 md:aspect-square md:w-[25vw] md:border-r md:p-[2vw] ${index >= 0 && index < 3 ? 'md:border-t' : ''} ${index % 3 === 0 ? 'md:border-l' : ''} `}
          >
            <div class="mb-4 flex shrink-0">
              {review.imageUrl ? (
                <Image
                  src={review.imageUrl}
                  alt={review.name}
                  inferSize
                  class="mr-8 h-28 w-28 rounded-full object-cover md:mr-[1.5vw] md:h-[7vw] md:w-[7vw]"
                />
              ) : (
                <div class="mr-8 flex h-28 w-28 items-center-safe justify-center rounded-full bg-gray-300 text-[6rem] leading-10 text-gray-600 md:mr-[1.5vw] md:h-[7vw] md:w-[7vw] md:text-[6vw]">
                  <div class="mt-[-0.8rem]">{review.name?.charAt(0).toUpperCase() || '?'}</div>
                </div>
              )}
              <div>
                <div class="stars-color text-lg tracking-widest md:text-[2.2vw] lg:text-[1.8vw]">
                  {'★'.repeat(review.rating) + '☆'.repeat(5 - review.rating)}
                </div>
                <div class="review-name mt-1 font-semibold" set:html={review.name} />
              </div>
            </div>
            <div class="review-description max-h-70 min-h-0 flex-1 overflow-auto pr-2 leading-snug">
              {review.description}
            </div>
          </div>
        ))
      }
    </div>
  </section>
  <style>
    #scroll-title-wrapper {
      position: relative;
    }
    #scroll-title {
      transition: transform 0.1s linear;
    }
    @media (prefers-reduced-motion: reduce) {
      #scroll-title {
        transition: none;
      }
    }

    .hernia-container {
      padding: 2rem;
    }

    .hernia-title {
      font-size: clamp(2rem, 2.5vw, 2.5rem);
      line-height: 0.4;
      text-align: left;
      margin-bottom: 0.5rem;
    }

    .hernia-text {
      font-size: clamp(1.45rem, 1.875vw, 2.25rem);
      line-height: 1.2;
      text-align: left;
    }

    .benefits-title {
      font-size: 1.5rem;
      line-height: 0.8;
      text-align: left;
      margin-top: auto;
    }

    .benefits-description {
      margin-top: auto;
      margin-bottom: 2rem;
    }

    .review-name {
      font-size: clamp(1.5rem, 1.875vw, 2.25rem);
      line-height: 1;
    }

    .review-description {
      font-size: clamp(1rem, 1.146vw, 1.375rem);
    }

    .arrow-icon.rotated {
      transform: rotate(180deg);
    }

    @media (width >= 40rem) {
      .hernia-container {
        padding: clamp(0.3rem, 2vw, 1.5rem);
      }

      .benefits.hernia-container {
        position: relative;
      }

      .benefits-title {
        font-size: clamp(2.5rem, 4vw, 5.25rem);
        font-weight: 500;
        position: absolute;
        top: 30%;
        left: 0;
        right: 0;
        padding: 0 clamp(0.3rem, 2vw, 1.5rem);
        margin-top: 0;
      }

      .benefits-description {
        position: absolute;
        top: 65%;
        left: 0;
        right: 0;
        padding: 0 clamp(0.3rem, 2vw, 1.5rem);
        margin-bottom: clamp(2rem, 3vw, 4rem);
        margin-top: 0;
      }

      .responsive-button-text {
        font-size: clamp(1.5rem, 2.1875vw, 2.625rem);
        line-height: 1.2;
      }
    }

    .scroll-title {
      font-size: clamp(11.25rem, 25.52vw, 30.625rem);
    }
  </style>

  <script>
    const smallScreen = 768;
    document.addEventListener('astro:page-load', () => {
      const title = document.getElementById('scroll-title');
      if (!title) return;

      let ticking = false;
      let maxShift = 0; // maximum horizontal translation (negative)
      let startOffset = 0; // vertical position where effect begins
      let effectHeight = 0; // scroll distance over which full shift occurs

      const computeMetrics = () => {
        const vw = window.innerWidth;
        const rect = title.getBoundingClientRect();
        const fullWidth = title.scrollWidth; // intrinsic width (nowrap)
        // Scroll begins when title enters viewport; we want whole width to traverse
        maxShift = Math.min(0, vw - fullWidth - 32); // extra 32px buffer
        startOffset = window.scrollY + rect.top - window.innerHeight * 0.6; // start a bit before center
        effectHeight = window.innerHeight * 1.5; // distance to complete animation
      };

      const update = () => {
        ticking = false;
        const y = window.scrollY;
        if (y <= startOffset) {
          title.style.transform = 'translateX(0)';
          return;
        }
        const progress = Math.min(1.5, (2.3 * (y - startOffset)) / effectHeight);
        const shift = maxShift * progress;
        title.style.transform = `translateX(${shift}px)`;
      };

      const requestTick = () => {
        if (!ticking) {
          ticking = true;
          requestAnimationFrame(update);
        }
      };

      window.addEventListener('scroll', requestTick, { passive: true });
      window.addEventListener('resize', () => {
        computeMetrics();
        requestTick();
      });
      computeMetrics();
      update();
    });
  </script>
</Layout>
